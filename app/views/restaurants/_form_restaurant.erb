
<%= simple_form_for(restaurant) do |r| %>

  <%= r.input :name %>

  <%= r.input :address, as: :text %>

  <%= r.input :phone_number %>

  <%= r.input :category, collection: ["chinese", "italian", "japanese", "french", "belgian"] %>
  <%= r.submit class: "btn btn-primary" %>
<% end %>
